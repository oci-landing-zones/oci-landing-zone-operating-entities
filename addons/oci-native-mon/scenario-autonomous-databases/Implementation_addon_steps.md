# Superseded implementation guide

The console-oriented foundation procedure has been replaced by the
[productized add-on workflow](../readme.md). Autonomous DBM/OPSI lifecycle uses
its separate OCI contract; do not apply the Base/ExaCS module contract.
